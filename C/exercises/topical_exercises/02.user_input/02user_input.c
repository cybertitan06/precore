#include <stdio.h>
#include <stdlib.h>

int main(int argc, char **argv) {

	//finish this program so that it prints the first argument. 
	//Fun fact: the first argument to every program is ALWAYS the name of the program itself. 
	//There will always be at least that one argument, even if the user doesn't provide any others
	

	printf("The name of this program is: %s\n", /*What goes here?*/);
	return 0;
}
