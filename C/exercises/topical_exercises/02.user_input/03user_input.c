#include <stdio.h>
#include <stdlib.h>

int main(int argc, char **argv) {

	//write a for loop that uses argc to iterate over argv and print out every argument provided by the user
	//to test your program, try running it with varying amounts of arguments
		
	for (int i = 0; /*what condition goes here?*/; i++){
		printf("argument number %d is %s\n",/*what variables should go here?*/);
		//%d is an integer
		//%s is a string
	}
	return 0;
}
