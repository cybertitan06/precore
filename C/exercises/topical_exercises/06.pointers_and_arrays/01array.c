#include <stdio.h>
#include <stdlib.h>

int main(int argc, char **argv) {
	int num_array[3] = {1,2,3};

	//use a for loop to print out each element of this array
	for (int i = 0; /*???*/; i++){
		printf("array element: %d\n",/*???*/);
	}
	return 0;
}
