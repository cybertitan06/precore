# Make a list of the primary colors: red, blue, yellow.
# Then use a function of the list to add purple, green, orange to the proper position
# between the primary colors.

colors = ['red', 'blue', 'yellow']
secondary_colors = ['purple', 'green', 'orange']

def mix_colors(primary1, primary2):
    if(primary1 == 'red' && primary2 == 'blue'):
        append('purple' between red & blue)


print(colors)
